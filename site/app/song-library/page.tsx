import catalog from '../data/catalog.json';
import unresolved from '../data/unresolved-recordings.json';
import { Footer, Header } from '../site-chrome';
import { SongLibrary } from './song-library';

export default function SongLibraryPage(){const playable=catalog.filter((item)=>item.fileId).length;const years=new Set(catalog.map((item)=>item.year));return <main><Header/><section className="page-hero library-hero"><p className="eyebrow">Singing Hoosiers song library</p><h1>Find the song <em>you remember.</em></h1><p>Search concert recordings by title and year. Long medleys are fully searchable, so one remembered phrase can lead you back to the whole performance.</p><div className="page-stats"><span><b>{catalog.length}</b> cataloged selections</span><span><b>{playable}</b> ready to play</span><span><b>{years.size}</b> recorded years</span><span><b>{unresolved.length}</b> awaiting year ID</span></div></section><SongLibrary data={catalog} unresolved={unresolved}/><section className="source-strip"><p><b>About this data:</b> Legacy titles and sequence come from the Singing Hoosier Archive Music spreadsheet. Newer 2000s-2010s recordings are dated from their filename suffixes and year-labeled source folders in the SHAC Drive. Obvious filename artifacts are cleaned for display while the original filename remains preserved in project data.</p><a href="https://docs.google.com/spreadsheets/d/1QrHUgG3IjB9DI5VGPySlW3QacBUdPy4UdIfOJBobc2M/edit" target="_blank" rel="noreferrer">View source spreadsheet ↗</a></section><Footer/></main>}
